/**
 * One thing to fill in.
 *
 * After deploying apps-script/Code.gs as a web app (Execute as Me, Access
 * Anyone), paste the /exec URL here, commit, and Netlify redeploys in about
 * 30 seconds.
 *
 * Until this is set, the form still captures every submission through Netlify
 * Forms, so nothing is lost. Setting it adds the Google Sheet row and the
 * GoHighLevel contact.
 *
 * For a dry run without committing, append ?endpoint=<url> to the page URL.
 */
window.AI_CONFIG = {
  leadEndpoint: ''
};
